import { Meta, StoryObj } from '@storybook/react'
import { RepoInput } from './RepoInput'

export default {
    title: 'components/RepoInput',
    component: RepoInput,
} as Meta<typeof RepoInput>

type Story = StoryObj<typeof RepoInput>

export const Default: Story = {}
